---
title: 0 -> 1 XMind Generator MCP
publishDate: 2025-01-22
read: 15
description: 详细指南：如何从零开始创建一个用于生成 XMind 思维导图的 MCP
tags:
  - MCP
  - XMind
  - TypeScript
  - Claude
  - 开发教程
img: "src/content/blog/Make_a_map-tools/index.mdx"
img_alt: "XMind Generator MCP 服务器开发教程"
---

## 前言

MCP（Model Context Protocol）是一个开放协议，允许 AI 助手安全地连接到外部数据源和工具。本教程将详细介绍如何创建一个专门用于生成 XMind 思维导图的 MCP 服务器。

## 1. 创建项目目录

首先，我们创建一个新的项目目录：

```bash
mkdir xmind-generator-mcp
cd xmind-generator-mcp
```

![项目目录创建](https://cdn.nlark.com/yuque/0/2025/png/40548187/1755846915097-420dc210-be67-4c8d-9080-530ae3e0431f.png)

## 2. 初始化 npm 项目

使用 npm 初始化一个新的 Node.js 项目：

```bash
npm init -y
```

这将创建一个默认的 `package.json` 文件。

## 3. 安装生产依赖

安装项目运行时必需的依赖包：

```bash
npm install @modelcontextprotocol/sdk@^1.9.0 jszip@^3.10.1 xmind-generator@^1.0.1 zod@^3.22.4
```

### 依赖说明：

- **`@modelcontextprotocol/sdk`** - MCP 协议的核心 SDK，用于与 Claude Desktop 通信
- **`jszip`** - 处理 ZIP 文件（XMind 文件本质是 ZIP 格式）
- **`xmind-generator`** - 核心库，专门用于生成 XMind 思维导图文件
- **`zod`** - TypeScript 数据验证库，确保输入参数正确

> 💡 **提示**：这些是运行时必需的依赖，打包后部署时也需要。

## 4. 安装开发依赖

安装开发和构建阶段需要的依赖：

```bash
npm install -D @types/node@^20.10.5 ts-node@^10.9.2 typescript@^5.3.3
```

### `-D` 参数说明：

- `-D` = `--save-dev` = 开发时依赖
- 这些包只在开发和构建阶段需要，部署时不需要

### 依赖说明：

- **`@types/node`** - Node.js 的 TypeScript 类型定义
- **`ts-node`** - 直接运行 TypeScript 文件
- **`typescript`** - TypeScript 编译器

## 5. 创建 TypeScript 配置文件

创建 TypeScript 配置文件：

```bash
touch tsconfig.json
```

将以下内容添加到 `tsconfig.json`：

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "esModuleInterop": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "declaration": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

![TypeScript 配置](https://cdn.nlark.com/yuque/0/2025/png/40548187/1755847373532-978398bb-ffeb-4ff5-8072-1c98f1065319.png)

## 6. 配置 package.json

打开 `package.json`，将整个内容替换为：

```json
{
  "name": "xmind-generator-mcp",
  "version": "0.1.1",
  "description": "MCP server for generating Xmind mind maps",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "xmind-generator-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mcp", "xmind", "mind-map", "claude", "anthropic", "llm"],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
```

### 重要配置说明：

- **`"type": "module"`** - 启用 ES 模块支持
- **`"bin"`** - 定义可执行文件路径，使包可以作为命令行工具使用
- **`"engines": { "node": ">=18" }`** - 指定 Node.js 版本要求

## 7. 创建源码目录和主文件

创建项目源码结构：

```bash
mkdir src
touch src/index.ts
```

## 8. 编写核心代码

打开 `src/index.ts` 进行核心代码编写：

![代码编写 1](https://cdn.nlark.com/yuque/0/2025/png/40548187/1755847732049-dc795b82-2b1d-48fc-a3a9-332a3032366c.png)

![代码编写 2](https://cdn.nlark.com/yuque/0/2025/png/40548187/1755847926331-c72034dc-8967-499c-ada6-0bf2dcf48af9.png)

![代码编写 3](https://cdn.nlark.com/yuque/0/2025/png/40548187/1755848010962-359a5777-7068-4b1d-95ce-c71b56065b8b.png)

![代码编写 4](https://cdn.nlark.com/yuque/0/2025/png/40548187/1755848186222-7791ef90-6cbd-4e91-a8c3-a1f2b260c3de.png)

以下是完整的 `src/index.ts` 代码：

```typescript
#!/usr/bin/env node

import { Server } from "@modelcontextprotocol/sdk/server/index.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import {
  CallToolRequestSchema,
  ErrorCode,
  ListToolsRequestSchema,
  McpError,
} from "@modelcontextprotocol/sdk/types.js";
import { z } from "zod";
import xmind from "xmind-generator";
import JSZip from "jszip";

// 定义数据模式
const MindMapTopicSchema = z.object({
  title: z.string(),
  note: z.string().optional(),
  labels: z.array(z.string()).optional(),
  markers: z.array(z.string()).optional(),
  children: z.array(z.lazy(() => MindMapTopicSchema)).optional(),
});

const MindMapSchema = z.object({
  title: z.string(),
  filename: z.string(),
  topics: z.array(MindMapTopicSchema),
});

type MindMapTopic = z.infer<typeof MindMapTopicSchema>;
type MindMap = z.infer<typeof MindMapSchema>;

// 创建 MCP 服务器
const server = new Server(
  {
    name: "xmind-generator-mcp",
    version: "0.1.1",
  },
  {
    capabilities: {
      tools: {},
    },
  }
);

// 注册工具列表
server.setRequestHandler(ListToolsRequestSchema, async () => {
  return {
    tools: [
      {
        name: "generate_xmind",
        description: "生成 XMind 思维导图文件",
        inputSchema: {
          type: "object",
          properties: {
            mindmap: {
              type: "object",
              properties: {
                title: { type: "string", description: "思维导图标题" },
                filename: {
                  type: "string",
                  description: "输出文件名（不含扩展名）",
                },
                topics: {
                  type: "array",
                  description: "主题节点数组",
                  items: {
                    type: "object",
                    properties: {
                      title: { type: "string", description: "主题标题" },
                      note: { type: "string", description: "备注信息" },
                      labels: {
                        type: "array",
                        items: { type: "string" },
                        description: "标签数组",
                      },
                      markers: {
                        type: "array",
                        items: { type: "string" },
                        description: "标记数组",
                      },
                      children: { type: "array", description: "子主题数组" },
                    },
                    required: ["title"],
                  },
                },
              },
              required: ["title", "filename", "topics"],
            },
          },
          required: ["mindmap"],
        },
      },
    ],
  };
});

// 注册工具调用处理器
server.setRequestHandler(CallToolRequestSchema, async (request) => {
  if (request.params.name === "generate_xmind") {
    try {
      const { mindmap } = request.params.arguments as { mindmap: MindMap };

      // 验证输入数据
      const validatedMindMap = MindMapSchema.parse(mindmap);

      // 生成 XMind 数据
      const xmindData = await xmind(validatedMindMap);

      // 处理 XMind 数据（如果是 Buffer，转换为 base64）
      let result: string;
      if (Buffer.isBuffer(xmindData)) {
        result = xmindData.toString("base64");
      } else {
        result = xmindData;
      }

      return {
        content: [
          {
            type: "text",
            text: `XMind 文件已生成：${validatedMindMap.filename}.xmind`,
          },
          {
            type: "text",
            text: `文件数据（base64）：${result.substring(0, 100)}...`,
          },
        ],
      };
    } catch (error) {
      if (error instanceof z.ZodError) {
        throw new McpError(
          ErrorCode.InvalidParams,
          `Invalid mindmap data: ${error.errors.map((e) => `${e.path.join(".")}: ${e.message}`).join(", ")}`
        );
      }

      throw new McpError(
        ErrorCode.InternalError,
        `Failed to generate XMind: ${error instanceof Error ? error.message : String(error)}`
      );
    }
  }

  throw new McpError(
    ErrorCode.MethodNotFound,
    `Unknown tool: ${request.params.name}`
  );
});

// 启动服务器
async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
  console.error("XMind Generator MCP server running on stdio");
}

main().catch((error) => {
  console.error("Server error:", error);
  process.exit(1);
});
```

## 9. 测试编译

编译 TypeScript 代码：

```bash
npm run build
```

这个命令会：

1. 执行 TypeScript 编译器 (tsc)
2. 将 `src/index.ts` 编译到 `dist/index.js`
3. 如果有语法错误才会报错

## 10. 解决编译问题（如遇到）

如果 TypeScript 编译器没有全局安装，有两个解决方案：

### 方案一：使用 npx（推荐）

修改 `package.json` 中的 scripts：

```json
{
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js",
    "dev": "npx ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build"
  }
}
```

然后重新运行：

```bash
npm run build
```

### 方案二：全局安装 TypeScript

```bash
npm install -g typescript
```

## 11. 验证编译结果

检查编译是否成功：

```bash
ls -la dist/
```

你应该能看到 `dist/index.js` 文件已经生成。

## 12. 测试运行

测试编译后的服务器：

```bash
npm run start
```

### 预期结果：

- 程序会启动并等待 MCP 协议输入
- 不会有错误信息
- 光标会停在空白行（这是正常的，因为它在等待 Claude Desktop 的连接）

按 `Ctrl+C` 停止服务器。

## 13. 创建示例文件

创建一个示例文件来测试思维导图生成：

```bash
mkdir examples
touch examples/simple-mindmap.json
```

将以下内容添加到 `examples/simple-mindmap.json`：

```json
{
  "title": "项目计划",
  "filename": "project-plan",
  "topics": [
    {
      "title": "需求分析",
      "note": "收集和分析项目需求",
      "children": [
        {
          "title": "用户调研",
          "labels": ["优先级: 高"]
        },
        {
          "title": "竞品分析",
          "markers": ["Task.quarter"]
        }
      ]
    },
    {
      "title": "开发阶段",
      "children": [
        {
          "title": "前端开发",
          "markers": ["Arrow.refresh"]
        },
        {
          "title": "后端开发"
        }
      ]
    },
    {
      "title": "测试",
      "children": [
        {
          "title": "单元测试"
        },
        {
          "title": "集成测试"
        }
      ]
    }
  ]
}
```

## 14. 本地部署配置

配置 Claude Desktop 以使用我们的 MCP 服务器。

检查 Claude 配置目录是否存在：

```bash
ls -la ~/Library/Application\ Support/Claude/
```

如果目录不存在，创建它：

```bash
mkdir -p ~/Library/Application\ Support/Claude/
```

创建或编辑配置文件：

```bash
touch ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

将以下配置添加到 `claude_desktop_config.json`：

```json
{
  "mcpServers": {
    "xmind-generator": {
      "command": "node",
      "args": ["/path/to/your/xmind-generator-mcp/dist/index.js"]
    }
  }
}
```

> ⚠️ **重要**：请将 `/path/to/your/xmind-generator-mcp/` 替换为你的项目实际路径。

## 15. 验证部署

1. 重启 Claude Desktop
2. 在聊天中输入：帮我生成一个项目计划的思维导图
3. Claude 应该能够使用 XMind Generator 工具

## 高级功能扩展

### 添加更多 XMind 功能

你可以扩展服务器以支持更多 XMind 功能：

```typescript
// 添加样式设置
const StyleSchema = z.object({
  theme: z.string().optional(),
  color: z.string().optional(),
  fontSize: z.number().optional(),
});

// 扩展主题模式
const EnhancedMindMapTopicSchema = MindMapTopicSchema.extend({
  style: StyleSchema.optional(),
  image: z
    .object({
      src: z.string(),
      width: z.number().optional(),
      height: z.number().optional(),
    })
    .optional(),
});
```

### 添加导出格式支持

支持多种导出格式：

```typescript
// 在工具列表中添加新工具
{
  name: 'export_to_png',
  description: '将 XMind 导出为 PNG 图片',
  inputSchema: {
    // ... 定义输入参数
  }
}
```

## 故障排除

### 常见问题

**问题 1：编译错误**

- 检查 TypeScript 版本是否兼容
- 确保 `tsconfig.json` 配置正确

**问题 2：MCP 连接失败**

- 验证 Claude Desktop 配置文件路径是否正确
- 检查 Node.js 版本是否 >= 18

**问题 3：XMind 生成失败**

- 检查输入数据格式是否符合规范
- 确保所有必需的字段都已提供

### 调试技巧

1. **启用详细日志**：

```typescript
// 在 main() 函数中添加
console.error("Debug: Server starting...");
```

2. **验证输入数据**：

```typescript
console.error("Debug: Input data:", JSON.stringify(mindmap, null, 2));
```

3. **检查文件路径**：

```bash
# 检查生成的文件
ls -la dist/
file dist/index.js
```

## 性能优化

### 代码优化建议

1. **使用流式处理**：对于大型思维导图，考虑使用流式处理
2. **缓存机制**：为重复生成的内容添加缓存
3. **错误处理**：完善错误处理和用户反馈

### 内存管理

```typescript
// 及时清理资源
process.on("SIGINT", () => {
  console.error("Received SIGINT, shutting down gracefully...");
  process.exit(0);
});
```

## 总结

恭喜！你已经成功创建了一个完整的 XMind Generator MCP 服务器。这个服务器具备以下功能：

- ✅ 生成 XMind 思维导图文件
- ✅ 支持复杂的层级结构
- ✅ 数据验证和错误处理
- ✅ Claude Desktop 集成
- ✅ TypeScript 类型安全

### 下一步建议

1. **扩展功能**：添加更多导出格式和样式选项
2. **优化性能**：实现缓存和流式处理
3. **完善文档**：添加更详细的 API 文档
4. **发布分享**：将包发布到 npm，分享给其他开发者

## 相关资源

- [MCP 官方文档](https://modelcontextprotocol.io)
- [XMind Generator NPM 包](https://www.npmjs.com/package/xmind-generator)
- [TypeScript 官方文档](https://www.typescriptlang.org)
- [Zod 数据验证库](https://zod.dev)
- [Claude Desktop 配置指南](https://docs.anthropic.com/claude/docs/mcp)

---

🎉 **享受使用你的 XMind Generator MCP 服务器吧！**

如果你在开发过程中遇到任何问题，欢迎查阅相关文档或提交 issue 寻求帮助。
